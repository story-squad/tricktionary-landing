import React from 'react';
import RenderAboutPage from './RenderAboutPage';

const AboutPageContainer = (): React.ReactElement => {
  return <RenderAboutPage />;
};

export default AboutPageContainer;
